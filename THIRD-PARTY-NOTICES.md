# Third-party notices

The MIT license in `LICENSE` covers this project's own code. One bundled asset carries
its own, separate license:

- **Geist font** (`public/fonts/Geist-Variable.woff2`) — © 2024 The Geist Project
  Authors, licensed under the SIL Open Font License 1.1. Full text:
  `public/fonts/OFL.txt`.

npm dependencies (`hono`, `jose`, and dev tooling) are not vendored into this repo —
each carries its own license within its own package once installed, per standard npm
convention.
